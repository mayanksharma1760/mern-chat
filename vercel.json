{
    "buildCommand":"cd client && yarn install && yarn vite build",
    "framework":"vite",
    "outputDirectory":"client/dist",
    "rewrites":[
        {
            "source":"/api/(.*)",
            "destination":"/api/index.js"
        }
    ]
}